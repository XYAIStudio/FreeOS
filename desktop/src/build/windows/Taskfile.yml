version: "3"

includes:
  common: ../Taskfile.yml

tasks:
  build:
    summary: Builds the Windows binary
    deps:
      - task: common:go:mod:tidy
      - task: common:build:frontend
      - task: common:generate:icons
    cmds:
      - task: generate:syso
        vars:
          ARCH: "{{.ARCH}}"
          VERSION: "{{.VERSION}}"
      - go build {{.BUILD_FLAGS}} -o "{{.BIN_DIR}}/{{.APP_NAME}}.exe"
      - cmd: powershell Remove-item *.syso
        platforms: [windows]
      - cmd: rm -f *.syso
        platforms: [linux, darwin]
    vars:
      BUILD_FLAGS: '{{if eq .PRODUCTION "true"}}-tags production -trimpath -buildvcs=false -ldflags="-w -s -H windowsgui"{{else}}-buildvcs=false -gcflags=all="-l"{{end}}'
      VERSION: '{{.VERSION | default "dev"}}'
    env:
      GOOS: windows
      CGO_ENABLED: "0"
      GOARCH: "{{.ARCH | default ARCH}}"
      PRODUCTION: '{{.PRODUCTION | default "false"}}'

  stage:portable:
    summary: Copies the green zip to bundled/portable.zip for go:embed
    cmds:
      - cmd: >-
          powershell -NoProfile -Command
          "if (-not (Test-Path '{{.PORTABLE_ZIP}}')) { throw 'portable zip not found: {{.PORTABLE_ZIP}}' };
          New-Item -ItemType Directory -Force bundled | Out-Null;
          Copy-Item -Force '{{.PORTABLE_ZIP}}' 'bundled/portable.zip'"
    vars:
          PORTABLE_ZIP: '{{.PORTABLE_ZIP | default (printf "../portable/release/FreeOS-portable-windows-%s-%s.zip" (.ARCH | default ARCH) (.VERSION | default "dev"))}}'

  stage:openxyos-runtime:
    summary: Stages the organization runtime from the portable bundle for NSIS
    cmds:
      - cmd: >-
          python "{{.ROOT_DIR}}/build/stage_openxyos_runtime.py" "{{.PORTABLE_ZIP}}"
          "{{.ROOT_DIR}}/build/windows/openxyos-runtime.zip"
        platforms: [windows]
      - cmd: >-
          python3 "{{.ROOT_DIR}}/build/stage_openxyos_runtime.py" "{{.PORTABLE_ZIP}}"
          "{{.ROOT_DIR}}/build/windows/openxyos-runtime.zip"
        platforms: [linux, darwin]
    vars:
      PORTABLE_ZIP: '{{.PORTABLE_ZIP | default (printf "../portable/release/FreeOS-portable-windows-%s-%s.zip" (.ARCH | default ARCH) (.VERSION | default "dev"))}}'

  maybe-stage:openxyos-runtime:
    summary: Stage openxyos-runtime.zip only when SHIP_OPENXYOS_RUNTIME=1 (transitional desktop bridge)
    status:
      - test "{{.SHIP_OPENXYOS_RUNTIME}}" != "1"
    cmds:
      - task: stage:openxyos-runtime
        vars:
          ARCH: "{{.ARCH}}"
          PORTABLE_ZIP: "{{.PORTABLE_ZIP}}"
          VERSION: "{{.VERSION}}"
    vars:
      SHIP_OPENXYOS_RUNTIME: '{{.SHIP_OPENXYOS_RUNTIME | default "0"}}'

  package:
    summary: Packages the NSIS installer. Node sidecar is opt-in (SHIP_OPENXYOS_RUNTIME=1, transitional).
    cmds:
      - task: stage:portable
        vars:
          ARCH: "{{.ARCH}}"
          PORTABLE_ZIP: "{{.PORTABLE_ZIP}}"
      - task: maybe-stage:openxyos-runtime
        vars:
          ARCH: "{{.ARCH}}"
          PORTABLE_ZIP: "{{.PORTABLE_ZIP}}"
          VERSION: "{{.VERSION}}"
          SHIP_OPENXYOS_RUNTIME: "{{.SHIP_OPENXYOS_RUNTIME}}"
      - task: build
        vars:
          PRODUCTION: "true"
          ARCH: "{{.ARCH}}"
          VERSION: "{{.VERSION}}"
      - task: create:nsis:installer
        vars:
          ARCH: "{{.ARCH}}"
          VERSION: "{{.VERSION}}"
          SHIP_OPENXYOS_RUNTIME: "{{.SHIP_OPENXYOS_RUNTIME}}"
      - cmd: >-
          powershell -NoProfile -Command
          "Remove-Item -Force '{{.BIN_DIR}}/{{.APP_NAME}}-Desktop-windows-{{.ARCH}}.zip' -ErrorAction SilentlyContinue;
          Remove-Item -Force '{{.BIN_DIR}}/{{.APP_NAME}}-Desktop-windows-{{.ARCH}}.exe' -ErrorAction SilentlyContinue;
          Remove-Item -Recurse -Force '{{.BIN_DIR}}/{{.APP_NAME}}-Desktop-windows-{{.ARCH}}' -ErrorAction SilentlyContinue;
          Remove-Item -Force 'bundled/portable.zip' -ErrorAction SilentlyContinue"
    vars:
      ARCH: "{{.ARCH | default ARCH}}"
      VERSION: '{{.VERSION | default "dev"}}'
      SHIP_OPENXYOS_RUNTIME: '{{.SHIP_OPENXYOS_RUNTIME | default "0"}}'
      PORTABLE_ZIP: '{{.PORTABLE_ZIP | default (printf "../portable/release/FreeOS-portable-windows-%s-%s.zip" .ARCH .VERSION)}}'
      ARCHIVE: '{{.BIN_DIR}}/{{.APP_NAME}}-desktop-windows-{{.ARCH}}-{{.VERSION}}.exe'

  create:nsis:installer:
    summary: Wraps FreeOS.exe in an NSIS installer (Program Files + shortcuts)
    dir: build/windows/nsis
    preconditions:
      - sh: makensis -VERSION
        msg: "makensis is required. Install NSIS (choco install nsis) and ensure it is on PATH."
    cmds:
      - wails3 generate webview2bootstrapper -dir "{{.ROOT_DIR}}/build/windows/nsis"
      - cmd: >-
          powershell -NoProfile -Command
          "Remove-Item -Force '{{.ROOT_DIR}}/{{.ARCHIVE}}' -ErrorAction SilentlyContinue;
          New-Item -ItemType Directory -Force '{{.ROOT_DIR}}/{{.BIN_DIR}}' | Out-Null"
      - |
        {{if eq OS "windows"}}
        makensis -INPUTCHARSET UTF8 {{.VERSION_NSIS_DEFINE}}-DARG_WAILS_{{.ARG_FLAG}}_BINARY="{{.ROOT_DIR}}\{{.BIN_DIR}}\{{.APP_NAME}}.exe" -DINSTALLER_OUTFILE="{{.ROOT_DIR}}\{{.ARCHIVE}}" project.nsi
        {{else}}
        makensis -INPUTCHARSET UTF8 {{.VERSION_NSIS_DEFINE}}-DARG_WAILS_{{.ARG_FLAG}}_BINARY="{{.ROOT_DIR}}/{{.BIN_DIR}}/{{.APP_NAME}}.exe" -DINSTALLER_OUTFILE="{{.ROOT_DIR}}/{{.ARCHIVE}}" project.nsi
        {{end}}
    vars:
      ARCH: '{{.ARCH | default ARCH}}'
      VERSION: '{{.VERSION | default "dev"}}'
      VERSION_NSIS_DEFINE: '{{if and .VERSION (ne .VERSION "dev")}}-DINFO_PRODUCTVERSION="{{.VERSION}}" {{end}}{{if eq .SHIP_OPENXYOS_RUNTIME "1"}}-DOPENXYOS_RUNTIME_ZIP_PRESENT {{end}}'
      SHIP_OPENXYOS_RUNTIME: '{{.SHIP_OPENXYOS_RUNTIME | default "0"}}'
      ARG_FLAG: '{{if eq (.ARCH | default ARCH) "amd64"}}AMD64{{else}}ARM64{{end}}'
      ARCHIVE: '{{.BIN_DIR}}/{{.APP_NAME}}-desktop-windows-{{.ARCH}}-{{.VERSION}}.exe'

  run:
    cmds:
      - "{{.BIN_DIR}}/{{.APP_NAME}}.exe"

  generate:syso:
    summary: Generates Windows .syso (linked from module root)
    dir: build
    cmds:
      - cmd: python stamp_version.py json windows/info.json windows/info.generated.json "{{.VERSION}}"
        platforms: [windows]
      - cmd: python3 stamp_version.py json windows/info.json windows/info.generated.json "{{.VERSION}}"
        platforms: [linux, darwin]
      - cmd: python stamp_version.py manifest windows/wails.exe.manifest windows/wails.exe.generated.manifest "{{.VERSION}}"
        platforms: [windows]
      - cmd: python3 stamp_version.py manifest windows/wails.exe.manifest windows/wails.exe.generated.manifest "{{.VERSION}}"
        platforms: [linux, darwin]
      - wails3 generate syso -arch {{.ARCH}} -icon windows/icon.ico -manifest windows/wails.exe.generated.manifest -info windows/info.generated.json -out ../wails_windows_{{.ARCH}}.syso
    vars:
      ARCH: "{{.ARCH | default ARCH}}"
      VERSION: '{{.VERSION | default "dev"}}'
